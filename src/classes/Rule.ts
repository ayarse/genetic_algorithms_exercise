/**
 * Ayas Nasih - S1600655
 * Villa College - BSCHCS (Jan 2020)
 */

export class Rule {
    condition: number[];
    output: number;

    constructor(condition: number[], output: number) {
        this.condition = condition;
        this.output = output;
    }
}